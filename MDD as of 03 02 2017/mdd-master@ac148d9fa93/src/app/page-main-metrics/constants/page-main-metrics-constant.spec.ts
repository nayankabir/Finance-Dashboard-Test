/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {PageMainMetricConstant} from "./page-main-metrics-constant";

describe('ReportFilterConstant', () => {
  it('should create an instance', () => {
    expect(new PageMainMetricConstant()).toBeTruthy();
  });
});
