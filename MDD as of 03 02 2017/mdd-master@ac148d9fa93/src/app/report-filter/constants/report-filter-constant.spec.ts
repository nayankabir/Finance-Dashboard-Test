/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {ReportFilterConstant} from './report-filter-constant';

describe('ReportFilterConstant', () => {
  it('should create an instance', () => {
    expect(new ReportFilterConstant()).toBeTruthy();
  });
});
